---
id: TPL-044
section: error-page
title: "Error Page"
theme_source: wezone-funilux
tags: [accessible, form, grid, error-page]
design_style: minimalist
features:
  - Extracted from working theme
  - Mobile-first responsive
  - Uses design tokens
responsive: [375, 768, 1024]
tokens_used: [wz-on-surface, wz-surface-pure, wz-surface, wz-radius, wz-surface-cool, wz-outline, wz-radius-card, wz-primary]
date: 2026-05-26
---

## Preview

Extracted from `wezone-funilux` theme.

See: `.claude/blueprint/pages/37-*.md`

## Files

| File | Role |
|------|------|
| 404.php | wezone-funilux template |

## Code — PHP

```php
<!-- 404.php -->
<?php
declare(strict_types=1);

if ( ! defined( 'ABSPATH' ) ) { exit; }

get_header();

$phone    = wz_config( 'phone', '' );
$email    = wz_config( 'email', '' );
$zalo_url = wz_config( 'zalo_url', '' );
$categories = function_exists( 'wz_get_categories' ) ? array_slice( wz_get_categories(), 0, 4 ) : [];
?>

<div class="bg-[var(--wz-surface)] min-h-screen">
<div class="max-w-[1320px] mx-auto px-5 md:px-[20px] py-8 md:py-16">

    <!-- Hero 404 -->
    <div class="text-center">
        <p class="text-[120px] md:text-[180px] font-bold leading-none text-[var(--wz-primary)]/10 select-none">404</p>
        <div class="-mt-16 md:-mt-20">
            <span class="material-symbols-outlined text-[56px] text-[var(--wz-primary)]" aria-hidden="true">search_off</span>
            <h1 class="text-xl md:text-2xl font-bold text-[var(--wz-primary)] mt-3">Trang không tồn tại</h1>
            <p class="text-sm text-[var(--wz-outline)] mt-2 max-w-md mx-auto">Trang bạn đang tìm kiếm có thể đã bị xóa, đổi tên hoặc tạm thời không khả dụng</p>
        </div>
    </div>

    <!-- Search -->
    <form action="<?php echo esc_url( home_url( '/' ) ); ?>" method="get" class="max-w-lg mx-auto mt-8">
        <div class="flex items-center border border-[var(--wz-outline)]/20 rounded-[var(--wz-radius)] overflow-hidden focus-within:border-[var(--wz-primary)] focus-within:ring-1 focus-within:ring-[var(--wz-primary)] transition">
            <input type="search" name="s" placeholder="Tìm kiếm sản phẩm, danh mục..."
                   class="flex-1 px-4 py-3 text-sm outline-none bg-transparent">
            <button type="submit" class="px-4 py-3 bg-[var(--wz-primary)] text-white hover:brightness-110 transition">
                <span class="material-symbols-outlined text-[20px]" aria-hidden="true">search</span>
            </button>
        </div>
    </form>

    <!-- Quick links -->
    <div class="flex flex-wrap justify-center gap-3 mt-6">
        <a href="<?php echo esc_url( home_url( '/' ) ); ?>" class="px-5 py-2.5 bg-[var(--wz-primary)] text-white rounded-[var(--wz-radius)] text-sm font-semibold hover:brightness-110 transition">Trang chủ</a>
        <a href="<?php echo esc_url( wz_get_shop_url() ); ?>" class="px-5 py-2.5 border border-[var(--wz-primary)] text-[var(--wz-primary)] rounded-[var(--wz-radius)] text-sm font-semibold hover:bg-[var(--wz-primary)]/5 transition">Xem sản phẩm</a>
        <a href="<?php echo esc_url( home_url( '/lien-he/' ) ); ?>" class="px-5 py-2.5 border border-[var(--wz-outline)]/30 text-[var(--wz-on-surface)] rounded-[var(--wz-radius)] text-sm font-semibold hover:bg-[var(--wz-surface-cool)] transition">Liên hệ</a>
    </div>

    <!-- Categories -->
    <?php if ( ! empty( $categories ) ) : ?>
        <div class="mt-10">
            <h2 class="text-lg font-bold text-[var(--wz-primary)] text-center mb-4">Danh mục nổi bật</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
                <?php foreach ( $categories as $cat ) : ?>
                    <a href="<?php echo esc_url( $cat['url'] ?? home_url( '/category/' . ( $cat['slug'] ?? '' ) . '/' ) ); ?>"
                       class="bg-[var(--wz-surface-pure)] rounded-[var(--wz-radius-card)] p-4 text-center shadow-[0_4px_24px_rgba(27,58,122,0.08)] hover:shadow-lg transition">
                        <span class="material-symbols-outlined text-[32px] text-[var(--wz-primary)]" aria-hidden="true">category</span>
                        <p class="text-sm font-semibold text-[var(--wz-on-surface)] mt-2"><?php echo esc_html( $cat['name'] ?? '' ); ?></p>
                    </a>
                <?php endforeach; ?>
            </div>
        </div>
    <?php endif; ?>

    <!-- Support strip -->
    <div class="mt-10 bg-[var(--wz-surface-pure)] rounded-[var(--wz-radius-card)] p-4 shadow-[0_4px_24px_rgba(27,58,122,0.08)]">
        <div class="flex flex-col md:flex-row items-center justify-center gap-4 md:gap-8">
            <a href="tel:<?php echo esc_attr( preg_replace( '/\s+/', '', $phone ) ); ?>" class="flex items-center gap-2 text-sm text-[var(--wz-on-surface)] hover:text-[var(--wz-primary)] transition">
                <span class="material-symbols-outlined text-[20px] text-[var(--wz-primary)]" aria-hidden="true">call</span>
                <span class="font-semibold"><?php echo esc_html( $phone ); ?></span>
            </a>
            <a href="mailto:<?php echo esc_attr( $email ); ?>" class="flex items-center gap-2 text-sm text-[var(--wz-on-surface)] hover:text-[var(--wz-primary)] transition">
                <span class="material-symbols-outlined text-[20px] text-[var(--wz-primary)]" aria-hidden="true">email</span>
                <span class="font-semibold"><?php echo esc_html( $email ); ?></span>
            </a>
            <a href="<?php echo esc_url( $zalo_url ); ?>" class="flex items-center gap-2 text-sm text-[var(--wz-on-surface)] hover:text-[var(--wz-primary)] transition">
                <span class="material-symbols-outlined text-[20px] text-[var(--wz-primary)]" aria-hidden="true">chat</span>
                <span class="font-semibold">Chat Zalo</span>
            </a>
        </div>
    </div>

</div>
</div>

<?php get_footer(); ?>
```

## Code — CSS

```css
/* No inline CSS — uses design tokens */
```

## Code — JS

```js
// No inline JS
```

## Usage Notes

- **Dependencies:** Wezone Commer (`wz_*` functions)
- **Tokens:** `--wz-on-surface`, `--wz-surface-pure`, `--wz-surface`, `--wz-radius`, `--wz-surface-cool` + 3 more
- **Breakpoints:** Mobile-first (375px → 768px → 1024px)
- **Extracted from:** wezone-funilux
