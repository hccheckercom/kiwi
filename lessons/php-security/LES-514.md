---
id: LES-514
severity: CRITICAL
category: php-security
title: 'REST API Missing Permission Callback'
tags: [plugin,rest-api,security]
date: 2026-05-24
scan:
  type: "presence"
  pattern: 'register_rest_route\([^)]+\)'
  scope: "**/*.php"
  context_guard:
    pattern: "permission_callback|@kiwi-ignore\\s+LES-514"
    lines_before: 0
    lines_after: 3
---

## Bad
```php
register_rest_route( 'wezone/v1', '/admin/settings', [
    'methods' => 'POST',
    'callback' => [ $this, 'save_settings' ],
] );
```

## Good
```php
register_rest_route( 'wezone/v1', '/admin/settings', [
    'methods' => 'POST',
    'callback' => [ $this, 'save_settings' ],
    'permission_callback' => [ $this, 'check_admin_permission' ],
] );

public function check_admin_permission(): bool {
    return current_user_can( 'manage_options' );
}
```

## Why
REST routes without permission_callback are publicly accessible. Attackers can call admin-only endpoints without authentication.

## Grep
`register_rest_route\([^)]+\)(?![^{]*permission_callback)` — in `**/*.php`
