---
id: LES-667
severity: CRITICAL
category: security
title: 'JWT Decode Without Verification'
tags: [both]
source: "gap-detect from python.json"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'jwt\.decode.*verify.*False'
  scope: "**/*.py"
---

## Bad
```php
payload = jwt.decode(token, options={"verify_signature": False})
```

## Good
```php
payload = jwt.decode(token, key, algorithms=["HS256"])
```

## Why
Skipping JWT verification allows forged tokens to be accepted

## Grep
`jwt\.decode.*verify.*False` — in `**/*.py`
