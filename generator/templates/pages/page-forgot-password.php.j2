<?php
/**
 * Template Name: Forgot Password
 */

if ( ! function_exists( 'wezone_is_active' ) ) {
	return;
}
?>

<?php get_header(); ?>

<main id="main" class="site-main">
	<div class="container mx-auto px-4 py-12">
		<div class="max-w-md mx-auto">
			<h1 class="text-2xl font-bold text-center mb-4">Quên mật khẩu</h1>
			<p class="text-gray-600 text-center mb-8">Nhập email đã đăng ký, chúng tôi sẽ gửi link đặt lại mật khẩu.</p>

			<form method="POST" action="<?php echo esc_url( home_url( '/api/auth/forgot-password' ) ); ?>" class="space-y-4">
				<?php wp_nonce_field( 'wz_forgot_password_nonce' ); ?>

				<div>
					<label for="reset_email" class="block text-sm font-medium mb-1">Email</label>
					<input type="email" id="reset_email" name="email" required
					       class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-transparent"
					       placeholder="email@example.com">
				</div>

				<button type="submit"
				        class="w-full px-6 py-3 bg-primary text-white rounded-lg font-medium hover:opacity-90 transition-opacity">
					Gửi link đặt lại
				</button>
			</form>

			<p class="text-center text-sm text-gray-600 mt-6">
				<a href="<?php echo esc_url( home_url( '/login' ) ); ?>" class="text-primary hover:underline">Quay lại đăng nhập</a>
			</p>
		</div>
	</div>
</main>

<?php get_footer(); ?>