---
id: LES-616
title: Bare except clause swallows all errors silently
category: python
severity: CRITICAL
platform: python
tags: [python, error-handling, debugging]
scan:
  type: presence
  pattern: 'except\s*:'
  scope: '**/*.py'
  exclude: ['**/test_*.py']
  exclude_line: 'except\\s+\\w'
---

## Bad

```python
try:
    data = json.loads(raw)
except:
    pass
```

## Good

```python
try:
    data = json.loads(raw)
except (json.JSONDecodeError, TypeError) as e:
    logger.warning("Failed to parse JSON: %s", e)
    data = {}
```

## Why

Bare `except:` catches **everything** including `SystemExit`, `KeyboardInterrupt`, and `GeneratorExit`. This makes debugging nearly impossible — errors vanish silently. Always catch specific exception types.

## Grep

```bash
rg 'except\s*:' --type py -g '!test_*.py'
```
