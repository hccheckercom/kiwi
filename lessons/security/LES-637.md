---
id: LES-637
severity: CRITICAL
category: security
title: 'os.system() — Use subprocess Instead'
tags: [both]
source: "auto-learned"
date: 2026-05-26
scan:
  type: "presence"
  pattern: '\bos\.system\s*\('
  scope: "**/*.py"
  exclude: ['**/test_*.py', 'agent/learn.py']
---

## Bad
```php
os.system(f"rm {filename}")
```

## Good
```php
subprocess.run(["rm", filename], check=True)
```

## Why
os.system() runs commands in a shell, enabling injection. Use subprocess with shell=False

## Grep
`\bos\.system\s*\(` — in `**/*.py`
