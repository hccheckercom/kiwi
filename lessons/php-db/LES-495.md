---
id: LES-495
severity: CRITICAL
category: php-db
title: 'Transaction khÃ´ng cÃ³ error handling â€” COMMIT thá»±c thi dÃ¹ logic fail'
tags: [plugin,database,transaction]
date: 2026-05-24
scan:
  type: "presence"
  pattern: 'START TRANSACTION.*?COMMIT(?!.*if\s*\(\s*\$ok\s*===\s*false)'
  scope: "**/*.php"
---

## Bad
```php
$wpdb->query( 'START TRANSACTION' );
$ok = $this->repo->log_transaction( $user_id, 'earn', $points, $balance );
$wpdb->query( 'COMMIT' ); // COMMIT dÃ¹ $ok = false
```

## Good
```php
$wpdb->query( 'START TRANSACTION' );
$ok = $this->repo->log_transaction( $user_id, 'earn', $points, $balance );
if ( ! $ok ) {
    $wpdb->query( 'ROLLBACK' );
    return;
}
$wpdb->query( 'COMMIT' );
```

## Why
Transaction START â†’ logic â†’ COMMIT mÃ  khÃ´ng check $ok result. Náº¿u log_transaction() fail nhÆ°ng váº«n COMMIT â†’ data inconsistent (balance table cÃ³ record nhÆ°ng points table khÃ´ng cÃ³).

## Grep
`START TRANSACTION.*?COMMIT(?!.*if\s*\(\s*\$ok\s*===\s*false)` — in `**/*.php`
