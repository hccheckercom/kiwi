---
id: LES-649
severity: SUGGEST
category: reliability
title: 'datetime.now() Without Timezone — Ambiguous Timestamps'
tags: [both]
source: "auto-learned"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'datetime\.now\(\)'
  scope: "**/*.py"
---

## Bad
```php
created_at = datetime.now()
```

## Good
```php
from datetime import datetime, timezone
created_at = datetime.now(timezone.utc)
```

## Why
datetime.now() uses local timezone which varies by server. Use timezone-aware datetimes

## Grep
`datetime\.now\(\)` — in `**/*.py`
