---
id: LES-651
severity: CRITICAL
category: security
title: 'SQL Injection via String Formatting'
tags: [both]
source: "auto-learned"
date: 2026-05-26
scan:
  type: "presence"
  pattern: '(execute|executemany)\s*\(\s*f["\']'
  scope: "**/*.py"
---

## Bad
```php
cursor.execute(f"SELECT * FROM users WHERE id = {user_id}")
```

## Good
```php
cursor.execute("SELECT * FROM users WHERE id = ?", (user_id,))
```

## Why
String formatting in SQL enables injection. Use parameterized queries

## Grep
`(execute|executemany)\s*\(\s*f["\']` — in `**/*.py`
