---
id: LES-496
severity: CRITICAL
category: wezone-api
title: 'REST API endpoint thiáº¿u input validation â€” accept negative/zero values'
tags: [plugin,api,validation]
date: 2026-05-24
scan:
  type: "presence"
  pattern: 'get_param\s*\(\s*['"](points|amount|quantity|user_id)['"]\s*\).*?\(int\)(?!.*if\s*\(.*?[<>]=?\s*0)'
  scope: "**/Api/*.php"
requires: wezone-commerce
---

## Bad
```php
public function admin_adjust_points( \WP_REST_Request $request ): \WP_REST_Response {
    $user_id = (int) $request->get_param( 'user_id' );
    $points  = (int) $request->get_param( 'points' );
    // KhÃ´ng check $user_id > 0 â†’ user_id=0 pass
    $this->manager->adjust( $user_id, $points, $note );
}
```

## Good
```php
public function admin_adjust_points( \WP_REST_Request $request ): \WP_REST_Response {
    $user_id = (int) $request->get_param( 'user_id' );
    $points  = (int) $request->get_param( 'points' );
    
    if ( $user_id <= 0 || $points === 0 ) {
        return new \WP_REST_Response( ['error' => 'Invalid input'], 400 );
    }
    
    $this->manager->adjust( $user_id, $points, $note );
}
```

## Why
REST API cast (int) nhÆ°ng khÃ´ng validate > 0. User gá»­i points=-1000 â†’ redeem() nháº­n negative â†’ balance tÄƒng thay vÃ¬ giáº£m. Hoáº·c user_id=0 â†’ query sai user.

## Grep
`get_param\s*\(\s*['"](points|amount|quantity|user_id)['"]\s*\).*?\(int\)(?!.*if\s*\(.*?[<>]=?\s*0)` — in `**/Api/*.php`
