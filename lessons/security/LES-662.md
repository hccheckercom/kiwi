---
id: LES-662
severity: HIGH
category: security
title: 'requests verify=False — SSL Bypass'
tags: [both]
source: "gap-detect from python.json"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'verify\s*=\s*False'
  scope: "**/*.py"
---

## Bad
```php
requests.get(url, verify=False)
```

## Good
```php
requests.get(url)  # verify=True is default
```

## Why
Disabling SSL verification exposes to man-in-the-middle attacks

## Grep
`verify\s*=\s*False` — in `**/*.py`
