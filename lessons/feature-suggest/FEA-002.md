---
id: FEA-002
severity: SUGGEST
platform: theme
category: feature-suggest
title: "Dynamic Product Attributes Filter — Bộ lọc thuộc tính từ DB"
tags: [theme]
source: "wezone-funilux"
date: 2026-05-18
scan:
  type: "absence"
  pattern: "wz_get_filter_attributes"
  scope: "template-parts/archive/sidebar-filters.php"
---

**Mô tả:**
- Sidebar filter query thuộc tính sản phẩm thật từ `wz_product_meta` table
- Chỉ hiển thị attributes có trong danh mục đang xem (filter by category_id)
- Dùng `wz_get_filter_attributes($category_id)` helper
**Detect pattern (absence):**
- `sidebar-filters.php` thiếu `wz_get_filter_attributes` → chưa có dynamic attributes
**Files cần có:**
- `inc/wz-shims.php` — function `wz_get_filter_attributes()`
- `template-parts/archive/sidebar-filters.php` — loop `$filter_attrs`
**Lợi ích:** Bộ lọc luôn đúng data, không mockup sai lệch
