---
id: LES-641
severity: CRITICAL
category: security
title: 'SQL Injection via String Formatting'
tags: [python, security, sql-injection]
source: "auto-learned"
date: 2026-05-26
platform: python
scan:
  type: "presence"
  pattern: '(execute|executemany)\s*\(\s*f["\x27](?!.*(?:CREATE|DROP|ALTER|PRAGMA)\s+TABLE)'
  scope: "**/*.py"
  exclude: ['**/test_*.py', 'agent/learn.py', 'agent/scoring.py', 'memory/coordination.py', 'deploy/wp_theme_activator.py', 'memory/migrate_*.py']
  exclude_line: 'CREATE TABLE|DROP TABLE|ALTER TABLE|PRAGMA table_info'
---

## Bad
```python
cursor.execute(f"SELECT * FROM users WHERE id = {user_id}")
```

## Good
```python
cursor.execute("SELECT * FROM users WHERE id = ?", (user_id,))
```

## Why
String formatting in SQL enables injection. Use parameterized queries

## Grep
```bash
rg '(execute|executemany)\s*\(\s*f["\x27]' --type py
```