---
id: LES-073
severity: HIGH
category: php-security
title: "REST get_param() dùng raw không cast type — type confusion risk"
tags: [plugin]
date: 2026-05-11
scan:
  type: "presence"
  pattern: '\$request->get_param\s*\([^)]+\)\s*;'
  scope: "packages/*/src/**/*.php"
  exclude: "vendor/**|tests/**"
  exclude_line: '\(string\)|\(int\)|\(float\)|\(bool\)|\(array\)|sanitize_|absint|intval|filter_var'
---

## Bad
```php
$id = $request->get_param('id');
$wpdb->get_row("SELECT * FROM table WHERE id = $id");
```

## Good
```php
$id = (int) $request->get_param('id');
$wpdb->get_row( $wpdb->prepare('SELECT * FROM table WHERE id = %d', $id) );
```

## Why
get_param() returns raw string. Without type cast, string '1 OR 1=1' passes through — type confusion risk.
