---
id: LES-656
severity: CRITICAL
category: security
title: 'yaml.load() Without SafeLoader — Code Execution Risk'
tags: [both]
source: "gap-detect from python.json"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'yaml\.load\('
  scope: "**/*.py"
---

## Bad
```php
data = yaml.load(f.read())
```

## Good
```php
data = yaml.safe_load(f.read())
```

## Why
yaml.load() with default Loader can execute arbitrary Python objects

## Grep
`yaml\.load\(` — in `**/*.py`
