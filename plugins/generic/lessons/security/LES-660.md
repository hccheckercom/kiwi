---
id: LES-660
severity: HIGH
category: security
title: 'random Module for Security — Use secrets Instead'
tags: [both]
source: "gap-detect from python.json"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'random\.(randint|choice|randrange)\('
  scope: "**/*.py"
---

## Bad
```php
token = ''.join(random.choice(chars) for _ in range(32))
```

## Good
```php
import secrets
token = secrets.token_urlsafe(32)
```

## Why
random module is predictable (PRNG). Use secrets module for security-sensitive values

## Grep
`random\.(randint|choice|randrange)\(` — in `**/*.py`
