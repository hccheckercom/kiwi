<?php
/**
 * Account Wishlist
 */

if ( ! function_exists( 'wezone_is_active' ) ) {
	return;
}

if ( ! is_user_logged_in() ) {
	wp_redirect( home_url( '/dang-nhap' ) );
	exit;
}

$user_id  = get_current_user_id();
$wishlist = function_exists( 'wz_get_wishlist' ) ? wz_get_wishlist( $user_id ) : array();
?>

<?php get_header(); ?>

<main id="main" class="site-main">
	<div class="container mx-auto px-4 py-8">

		<nav class="breadcrumb mb-6 text-sm" aria-label="Breadcrumb">
			<a href="<?php echo esc_url( home_url() ); ?>" class="text-gray-600 hover:text-primary">Trang chủ</a>
			<span class="mx-2" aria-hidden="true">/</span>
			<a href="<?php echo esc_url( home_url( '/tai-khoan' ) ); ?>" class="text-gray-600 hover:text-primary">Tài khoản</a>
			<span class="mx-2" aria-hidden="true">/</span>
			<span class="text-gray-900">Yêu thích</span>
		</nav>

		<div class="grid grid-cols-1 lg:grid-cols-4 gap-8">

			<aside class="lg:col-span-1">
				<?php get_template_part( 'template-parts/account/sidebar', null, array( 'active_tab' => 'wishlist' ) ); ?>
			</aside>

			<div class="lg:col-span-3">
				<h1 class="text-xl font-semibold mb-6">Sản phẩm yêu thích</h1>

				<?php if ( ! empty( $wishlist ) ) : ?>
				<div class="grid grid-cols-2 md:grid-cols-3 xl:grid-cols-4 gap-4">
					<?php foreach ( $wishlist as $product ) : ?>
					<div class="bg-white rounded-xl shadow-sm overflow-hidden group">
						<div class="relative">
							<a href="<?php echo esc_url( wz_get_permalink( $product ) ); ?>">
								<img src="<?php echo esc_url( $product['thumbnail'] ); ?>"
								     alt="<?php echo esc_attr( $product['name'] ); ?>"
								     class="w-full aspect-square object-cover group-hover:scale-105 transition-transform duration-300"
								     loading="lazy">
							</a>
							<button type="button"
							        class="absolute top-2 right-2 w-8 h-8 bg-white rounded-full shadow flex items-center justify-center hover:text-red-500 transition-colors wz-remove-wishlist"
							        data-product-id="<?php echo esc_attr( $product['id'] ); ?>"
							        aria-label="Xóa khỏi yêu thích">
								<span class="material-icons text-sm" aria-hidden="true">favorite</span>
							</button>
						</div>
						<div class="p-3">
							<a href="<?php echo esc_url( wz_get_permalink( $product ) ); ?>"
							   class="text-sm font-medium line-clamp-2 hover:text-primary transition-colors">
								<?php echo esc_html( $product['name'] ); ?>
							</a>
							<div class="flex items-center gap-2 mt-2">
								<span class="font-bold text-primary"><?php echo esc_html( wz_format_price( $product['price'] ) ); ?></span>
								<?php if ( ! empty( $product['regular_price'] ) && $product['regular_price'] > $product['price'] ) : ?>
								<span class="text-xs text-gray-400 line-through"><?php echo esc_html( wz_format_price( $product['regular_price'] ) ); ?></span>
								<?php endif; ?>
							</div>
							<button type="button"
							        class="mt-3 w-full py-1.5 bg-primary text-white text-sm rounded-lg hover:opacity-90 transition-opacity wz-add-to-cart"
							        data-product-id="<?php echo esc_attr( $product['id'] ); ?>">
								Thêm vào giỏ
							</button>
						</div>
					</div>
					<?php endforeach; ?>
				</div>
				<?php else : ?>
				<div class="bg-white rounded-xl shadow-sm p-12 text-center text-gray-500">
					<span class="material-icons text-5xl mb-3 block text-gray-300" aria-hidden="true">favorite_border</span>
					<p class="font-medium">Chưa có sản phẩm yêu thích</p>
					<a href="<?php echo esc_url( function_exists( 'wz_get_shop_url' ) ? wz_get_shop_url() : home_url( '/shop' ) ); ?>"
					   class="inline-block mt-4 px-6 py-2 bg-primary text-white rounded-lg text-sm hover:opacity-90 transition-opacity">
						Khám phá sản phẩm
					</a>
				</div>
				<?php endif; ?>
			</div>
		</div>
	</div>
</main>

<?php get_footer(); ?>
