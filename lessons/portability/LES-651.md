---
id: LES-651
severity: SUGGEST
category: portability
title: 'open() Without Encoding — Platform-Dependent Behavior'
tags: [both]
source: "auto-learned"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'open\([^)]+\)'
  scope: "**/*.py"
---

## Bad
```php
with open('data.txt') as f:
```

## Good
```php
with open('data.txt', encoding='utf-8') as f:
```

## Why
Default encoding varies by OS. Always specify encoding for text files

## Grep
`open\([^)]+\)` — in `**/*.py`
