<?php
/**
 * Template Name: Thank You
 */

if ( ! function_exists( 'wezone_is_active' ) ) {
	return;
}
?>

<?php get_header(); ?>

<main id="main" class="site-main">
	<div class="container mx-auto px-4 py-8">
		<?php
		$order_id = isset( $_GET['order_id'] ) ? absint( $_GET['order_id'] ) : 0;
		$order    = null;

		if ( $order_id && function_exists( 'wz_get_order' ) ) {
			$order = wz_get_order( $order_id );
		}
		?>

		<?php if ( $order ) : ?>
			<div class="max-w-2xl mx-auto text-center py-12">
				<div class="w-20 h-20 mx-auto mb-6 bg-green-100 rounded-full flex items-center justify-center">
					<svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
					</svg>
				</div>

				<h1 class="text-3xl font-bold mb-4">Đặt hàng thành công!</h1>
				<p class="text-gray-600 mb-2">Cảm ơn bạn đã mua hàng tại <?php echo esc_html( get_bloginfo( 'name' ) ); ?>.</p>
				<p class="text-gray-600 mb-8">Mã đơn hàng: <strong>#<?php echo esc_html( $order['order_number'] ); ?></strong></p>

				<!-- Order summary -->
				<div class="bg-white rounded-lg shadow-sm p-6 text-left mb-8">
					<h2 class="text-lg font-bold mb-4">Chi tiết đơn hàng</h2>

					<div class="divide-y divide-gray-200">
						<?php foreach ( $order['items'] as $item ) : ?>
							<div class="flex justify-between py-3">
								<div>
									<span class="font-medium"><?php echo esc_html( $item['name'] ); ?></span>
									<span class="text-gray-500 ml-2">x<?php echo esc_html( (int) $item['quantity'] ); ?></span>
								</div>
								<span><?php echo esc_html( wz_format_price( $item['total'] ) ); ?></span>
							</div>
						<?php endforeach; ?>
					</div>

					<hr class="my-4">

					<div class="space-y-2 text-sm">
						<div class="flex justify-between">
							<span class="text-gray-600">Tạm tính</span>
							<span><?php echo esc_html( wz_format_price( $order['subtotal'] ) ); ?></span>
						</div>
						<div class="flex justify-between">
							<span class="text-gray-600">Phí vận chuyển</span>
							<span><?php echo esc_html( wz_format_price( $order['shipping_fee'] ) ); ?></span>
						</div>
						<?php if ( ! empty( $order['discount'] ) && $order['discount'] > 0 ) : ?>
							<div class="flex justify-between text-green-600">
								<span>Giảm giá</span>
								<span>-<?php echo esc_html( wz_format_price( $order['discount'] ) ); ?></span>
							</div>
						<?php endif; ?>
					</div>

					<hr class="my-4">

					<div class="flex justify-between text-lg font-bold">
						<span>Tổng cộng</span>
						<span class="text-primary"><?php echo esc_html( wz_format_price( $order['total'] ) ); ?></span>
					</div>
				</div>

				<!-- Shipping info -->
				<div class="bg-white rounded-lg shadow-sm p-6 text-left mb-8">
					<h2 class="text-lg font-bold mb-4">Thông tin giao hàng</h2>
					<p class="text-gray-700"><?php echo esc_html( $order['shipping_name'] ); ?></p>
					<p class="text-gray-600"><?php echo esc_html( $order['shipping_phone'] ); ?></p>
					<p class="text-gray-600"><?php echo esc_html( $order['shipping_address'] ); ?></p>
				</div>

				<div class="flex flex-col sm:flex-row gap-4 justify-center">
					<a href="<?php echo esc_url( home_url( '/account/orders' ) ); ?>"
					   class="px-6 py-3 bg-primary text-white rounded-lg hover:opacity-90 transition-opacity text-center">
						Theo dõi đơn hàng
					</a>
					<a href="<?php echo esc_url( home_url( '/shop' ) ); ?>"
					   class="px-6 py-3 border border-gray-300 rounded-lg hover:bg-gray-50 transition-colors text-center">
						Tiếp tục mua sắm
					</a>
				</div>
			</div>

		<?php else : ?>
			<div class="max-w-2xl mx-auto text-center py-12">
				<h1 class="text-2xl font-bold mb-4">Không tìm thấy đơn hàng</h1>
				<p class="text-gray-600 mb-6">Đơn hàng không tồn tại hoặc bạn không có quyền xem.</p>
				<a href="<?php echo esc_url( home_url( '/shop' ) ); ?>"
				   class="inline-block px-6 py-3 bg-primary text-white rounded-lg hover:opacity-90 transition-opacity">
					Quay lại cửa hàng
				</a>
			</div>
		<?php endif; ?>
	</div>
</main>

<?php get_footer(); ?>