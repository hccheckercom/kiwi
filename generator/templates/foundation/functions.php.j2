<?php
/**
 * {{ shop_name }} Theme Functions
 *
 * Entry point for theme functionality.
 * Generated by Kiwi Generator Phase 1
 *
 * @package {{ theme_slug }}
 * @generated {{ timestamp }}
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Theme constants
define( '{{ theme_slug_underscore|upper }}_VERSION', '1.0.0' );
define( '{{ theme_slug_underscore|upper }}_DIR', get_template_directory() );
define( '{{ theme_slug_underscore|upper }}_URI', get_template_directory_uri() );

// Load core modules
require_once {{ theme_slug_underscore|upper }}_DIR . '/inc/helpers.php';
require_once {{ theme_slug_underscore|upper }}_DIR . '/inc/security.php';
require_once {{ theme_slug_underscore|upper }}_DIR . '/inc/setup.php';
require_once {{ theme_slug_underscore|upper }}_DIR . '/inc/seo.php';

// Load design system
if ( file_exists( {{ theme_slug_underscore|upper }}_DIR . '/inc/design-system.php' ) ) {
	require_once {{ theme_slug_underscore|upper }}_DIR . '/inc/design-system.php';
}

// Enqueue styles and scripts
add_action( 'wp_enqueue_scripts', '{{ theme_slug_underscore }}_enqueue_assets' );
function {{ theme_slug_underscore }}_enqueue_assets() {
	// Tailwind CSS (compiled)
	wp_enqueue_style(
		'{{ theme_slug }}-tailwind',
		{{ theme_slug_underscore|upper }}_URI . '/assets/css/tailwind.css',
		[],
		{{ theme_slug_underscore|upper }}_VERSION
	);

	// Main JS
	wp_enqueue_script(
		'{{ theme_slug }}-main',
		{{ theme_slug_underscore|upper }}_URI . '/assets/js/main.js',
		[],
		{{ theme_slug_underscore|upper }}_VERSION,
		true
	);

	// Localize script with theme config
	wp_localize_script(
		'{{ theme_slug }}-main',
		'wzTheme',
		[
			'ajaxUrl' => admin_url( 'admin-ajax.php' ),
			'nonce'   => wp_create_nonce( '{{ theme_slug }}_nonce' ),
			'config'  => function_exists( 'wz_config' ) ? wz_config() : [],
		]
	);
}