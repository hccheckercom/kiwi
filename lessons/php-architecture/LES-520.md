---
id: LES-520
severity: HIGH
category: php-architecture
title: 'Hook Registration Without Priority Control'
tags: [plugin,architecture,hooks]
date: 2026-05-24
scan:
  type: "presence"
  pattern: 'add_action\([^,]+,[^,]+\)\s*;'
  scope: "packages/*/src/Plugin.php"
---

## Bad
```php
add_action( 'init', [ $this, 'register_post_types' ] );
add_action( 'init', [ $this, 'register_taxonomies' ] );
```

## Good
```php
add_action( 'init', [ $this, 'register_post_types' ], 5 );
add_action( 'init', [ $this, 'register_taxonomies' ], 10 );
```

## Why
Hooks without explicit priority use default 10. When multiple plugins hook same action, execution order is unpredictable. Always specify priority for critical hooks (init, rest_api_init, plugins_loaded).

## Grep
`add_action\([^,]+,[^,]+\)\s*;` — in `packages/*/src/Plugin.php`
