---
id: LES-654
severity: HIGH
category: security
title: 'Unvalidated Redirect — Open Redirect Risk'
tags: [both]
source: "auto-learned"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'redirect\(request\.'
  scope: "**/*.py"
---

## Bad
```php
return redirect(request.args.get('next'))
```

## Good
```php
from urllib.parse import urlparse
next_url = request.args.get('next', '/')
if urlparse(next_url).netloc == '':
    return redirect(next_url)
```

## Why
Unvalidated redirects can be exploited for phishing by pointing to malicious sites

## Grep
`redirect\(request\.` — in `**/*.py`
