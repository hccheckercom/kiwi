---
id: LES-635
severity: CRITICAL
category: security
title: 'pickle.load — Arbitrary Code Execution Risk'
tags: [both]
source: "auto-learned"
date: 2026-05-26
scan:
  type: "presence"
  pattern: 'pickle\.(load|loads)\s*\('
  scope: "**/*.py"
  exclude: ['**/test_*.py', 'agent/learn.py', 'agent/context.py', 'learning/embeddings.py', 'learning/ml_trainer.py']
---

## Bad
```php
data = pickle.load(open("data.pkl", "rb"))
```

## Good
```php
import json
data = json.load(open("data.json"))
```

## Why
pickle can execute arbitrary code during deserialization. Use json for untrusted data

## Grep
`pickle\.(load|loads)\s*\(` — in `**/*.py`
