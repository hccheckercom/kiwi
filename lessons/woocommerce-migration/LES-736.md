---
id: LES-736
title: "WooCommerce template override pattern — dùng wezone-templates/"
severity: HIGH
platform: wp
category: woocommerce-migration
tags: [theme]
scan:
  type: presence
  pattern: "woocommerce/|wc-template|woocommerce_content|woocommerce_get_sidebar|woocommerce_output"
  scope: "**/*.php"
  exclude: "vendor/**|node_modules/**"
  message: "WooCommerce template structure không áp dụng — dùng wezone-templates/"
created: "2026-05-28"
---

## Pattern

Developer tạo folder `woocommerce/` trong theme để override WC templates,
hoặc dùng WC template functions trong theme files.

## Bad

```
mytheme/
├── woocommerce/                    ← WC template override folder
│   ├── single-product.php
│   ├── archive-product.php
│   ├── cart/
│   │   ├── cart.php
│   │   └── cart-totals.php
│   ├── checkout/
│   │   ├── form-checkout.php
│   │   └── form-billing.php
│   ├── myaccount/
│   │   └── my-account.php
│   └── loop/
│       └── price.php
```

```php
// In theme functions.php
remove_action( 'woocommerce_sidebar', 'woocommerce_get_sidebar' );
remove_action( 'woocommerce_before_main_content', 'woocommerce_output_content_wrapper' );
remove_action( 'woocommerce_after_main_content', 'woocommerce_output_content_wrapper_end' );

// In theme template
woocommerce_content();
wc_get_template_part( 'content', 'product' );
```

## Good

```
mytheme/
├── wezone-templates/               ← Wezone template folder
│   ├── product/
│   │   ├── single.php
│   │   └── archive.php
│   ├── cart/
│   │   ├── cart.php
│   │   └── totals.php
│   ├── checkout/
│   │   ├── form.php
│   │   └── billing.php
│   └── account/
│       └── dashboard.php
├── template-parts/                 ← Theme partials
│   ├── product/
│   │   ├── card.php
│   │   └── price.php
│   └── loop/
│       └── product-loop.php
```

```php
// In theme — load Wezone template
wz_get_template( 'product/single.php', array( 'product' => $product ) );
wz_get_template_part( 'product', 'card' );

// Or standard WordPress
get_template_part( 'template-parts/product/card', '', array( 'product' => $product ) );
```

## Why

- WooCommerce template override system tìm files trong `yourtheme/woocommerce/` → Wezone không scan folder này
- `woocommerce_content()` function không tồn tại
- Wezone template system dùng `wezone-templates/` folder
- Template parts nên dùng `template-parts/` (WordPress standard) hoặc `wezone-templates/`
- Wezone KHÔNG dùng WC's template version checking system
