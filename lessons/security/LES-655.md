---
id: LES-655
severity: CRITICAL
category: security
title: 'exec() Usage — Arbitrary Code Execution'
tags: [both]
source: "gap-detect from python.json"
date: 2026-05-26
scan:
  type: "presence"
  pattern: '\bexec\s*\('
  scope: "**/*.py"
---

## Bad
```php
exec(user_code)
```

## Good
```php
# Use restricted execution or AST-based evaluation
```

## Why
exec() runs arbitrary code strings, enabling code injection attacks

## Grep
`\bexec\s*\(` — in `**/*.py`
