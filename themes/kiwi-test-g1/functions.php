<?php
/**
 * Kiwi Test Shop Theme Functions
 *
 * Entry point for theme functionality.
 * Generated by Kiwi Generator Phase 1
 *
 * @package kiwi-test-g1
 * @generated 2026-05-25T10:36:11.581596
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// Theme constants
define( 'KIWI_TEST_G1_VERSION', '1.0.0' );
define( 'KIWI_TEST_G1_DIR', get_template_directory() );
define( 'KIWI_TEST_G1_URI', get_template_directory_uri() );

// Load core modules
require_once KIWI_TEST_G1_DIR . '/inc/helpers.php';
require_once KIWI_TEST_G1_DIR . '/inc/security.php';
require_once KIWI_TEST_G1_DIR . '/inc/setup.php';
require_once KIWI_TEST_G1_DIR . '/inc/seo.php';

// Load design system
if ( file_exists( KIWI_TEST_G1_DIR . '/inc/design-system.php' ) ) {
	require_once KIWI_TEST_G1_DIR . '/inc/design-system.php';
}

// Enqueue styles and scripts
add_action( 'wp_enqueue_scripts', 'kiwi_test_g1_enqueue_assets' );
function kiwi_test_g1_enqueue_assets() {
	// Tailwind CSS (compiled)
	wp_enqueue_style(
		'kiwi-test-g1-tailwind',
		KIWI_TEST_G1_URI . '/assets/css/tailwind.css',
		[],
		KIWI_TEST_G1_VERSION
	);

	// Main JS
	wp_enqueue_script(
		'kiwi-test-g1-main',
		KIWI_TEST_G1_URI . '/assets/js/main.js',
		[],
		KIWI_TEST_G1_VERSION,
		true
	);

	// Localize script with theme config
	wp_localize_script(
		'kiwi-test-g1-main',
		'wzTheme',
		[
			'ajaxUrl' => admin_url( 'admin-ajax.php' ),
			'nonce'   => wp_create_nonce( 'kiwi-test-g1_nonce' ),
			'config'  => function_exists( 'wz_config' ) ? wz_config() : [],
		]
	);
}
