<?php
/**
 * Account Returns
 */

if ( ! function_exists( 'wezone_is_active' ) ) {
	return;
}

if ( ! is_user_logged_in() ) {
	wp_redirect( home_url( '/dang-nhap' ) );
	exit;
}

$user_id = get_current_user_id();
$returns = function_exists( 'wz_get_user_returns' ) ? wz_get_user_returns( $user_id ) : array();
?>

<?php get_header(); ?>

<main id="main" class="site-main">
	<div class="container mx-auto px-4 py-8">

		<nav class="breadcrumb mb-6 text-sm" aria-label="Breadcrumb">
			<a href="<?php echo esc_url( home_url() ); ?>" class="text-gray-600 hover:text-primary">Trang chủ</a>
			<span class="mx-2" aria-hidden="true">/</span>
			<a href="<?php echo esc_url( home_url( '/tai-khoan' ) ); ?>" class="text-gray-600 hover:text-primary">Tài khoản</a>
			<span class="mx-2" aria-hidden="true">/</span>
			<span class="text-gray-900">Đổi trả hàng</span>
		</nav>

		<div class="grid grid-cols-1 lg:grid-cols-4 gap-8">

			<aside class="lg:col-span-1">
				<?php get_template_part( 'template-parts/account/sidebar', null, array( 'active_tab' => 'orders' ) ); ?>
			</aside>

			<div class="lg:col-span-3">
				<div class="flex items-center justify-between mb-6">
					<h1 class="text-xl font-semibold">Đổi trả hàng</h1>
					<a href="<?php echo esc_url( home_url( '/chinh-sach-doi-tra' ) ); ?>"
					   class="text-sm text-primary hover:underline">Xem chính sách đổi trả</a>
				</div>

				<?php if ( ! empty( $returns ) ) : ?>
				<div class="space-y-4">
					<?php foreach ( $returns as $return ) : ?>
					<div class="bg-white rounded-xl shadow-sm p-5">
						<div class="flex flex-wrap items-center justify-between gap-3 mb-3">
							<div>
								<span class="font-mono text-sm font-bold">Yêu cầu #<?php echo esc_html( $return['id'] ); ?></span>
								<span class="ml-2 text-sm text-gray-500">Đơn #<?php echo esc_html( $return['order_id'] ); ?></span>
							</div>
							<span class="inline-block px-2 py-0.5 rounded-full text-xs font-medium
								<?php
								$status_classes = array(
									'pending'   => 'bg-yellow-100 text-yellow-700',
									'approved'  => 'bg-blue-100 text-blue-700',
									'completed' => 'bg-green-100 text-green-700',
									'rejected'  => 'bg-red-100 text-red-700',
								);
								echo esc_attr( $status_classes[ $return['status'] ] ?? 'bg-gray-100 text-gray-700' );
								?>">
								<?php
								$status_labels = array(
									'pending'   => 'Chờ xử lý',
									'approved'  => 'Đã duyệt',
									'completed' => 'Hoàn tất',
									'rejected'  => 'Từ chối',
								);
								echo esc_html( $status_labels[ $return['status'] ] ?? $return['status'] );
								?>
							</span>
						</div>
						<p class="text-sm text-gray-600"><?php echo esc_html( $return['reason'] ); ?></p>
						<p class="text-xs text-gray-400 mt-2"><?php echo esc_html( date_i18n( 'd/m/Y', strtotime( $return['created_at'] ) ) ); ?></p>
					</div>
					<?php endforeach; ?>
				</div>
				<?php else : ?>
				<div class="bg-white rounded-xl shadow-sm p-12 text-center text-gray-500">
					<span class="material-icons text-5xl mb-3 block text-gray-300" aria-hidden="true">assignment_return</span>
					<p class="font-medium">Bạn chưa có yêu cầu đổi trả nào</p>
					<p class="text-sm mt-1">Liên hệ hotline <a href="tel:<?php echo esc_attr( wz_config( 'phone' ) ); ?>" class="text-primary hover:underline"><?php echo esc_html( wz_config( 'phone' ) ); ?></a> để được hỗ trợ</p>
				</div>
				<?php endif; ?>
			</div>
		</div>
	</div>
</main>

<?php get_footer(); ?>
